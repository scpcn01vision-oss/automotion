# automotion-v7 组建文档

> 生成日期：2026-08-04
> 定位：v7 新一代流程的组建蓝图。本文档判定旧 automotion（v6）及周边资产**哪些用、哪些不用、哪些新写**，覆盖全流程（镜头库 → 数据模型 → 匹配 → 工作台 → 渲染 → 最终合成），不只是镜头部分。
> 原则：单一代码源、中性镜头库、项目数据分离、可复现匹配、可审查流程。

---

## 1. 全流程总览（v7 数据流）

```
第一步：文案处理 + 录音处理（并行）
  ├ 文案：文案 → 分段（参考 013 经验，段含 phraseRange）→ 段文本列表
  └ 录音：口播录音 → 词级转录（字词语义 + 时间戳）→ 字幕分割 → 转录文件 + 字幕文件
  汇合：段文本 ↔ 词级转录对齐 → 每段真实时长（第一步定死）
第二步：工作台匹配定稿
  匹配（规则在 M3 定稿）→ 段×镜头×参数 + 字幕样式定稿 → storyboard.json（权威）
第三步：Remotion 整片预览（画面 + 字幕，无 BGM）——验收节点，不通过回第二步只重渲对应镜头
第四步：导出最终视频
```

- 工作台 = 定稿工具（浏览器网页），分「分镜定稿」「字幕样式」两环节：只负责段→镜头→参数 + 字幕样式
- Remotion = 预览与最终执行（整片预览 / 导出），不参与选镜头
- 镜头库 = 中性资产库，不含任何项目内容
- 时长基础：每段真实时长由第一步的转录确定；镜头时长 = 刚性（不可压缩）+ 弹性（可伸缩）**灵活组合**，总时长对齐段时长
- v7 当前阶段不引入 BGM

---

## 2. 资产判定总表

| 资产 | 判定 | 说明 |
|---|---|---|
| 163 个纸墨版镜头组件源码（纸墨版 140 + video-shotcraft 后加 23 个已纸墨化；均未参数化） | ✅ 用 | 来源：纸墨/src/compositions.bak；搬入 v7/lenses，全量注册，池划分 M2 定 |
| G 纸墨色板（颜色常量） | ✅ 用 | 从 Fixtures.tsx 拆出为独立文件 |
| engine/time.ts + useShotFrame | ✅ 用 | 时间轴引擎（刚性/弹性），测试过 |
| 命名表（lens-names.md） | ✅ 用 | id=组件名 + 中文名 + 组 |
| 旧静态分级（可用 11 / 疑似 17 / 不可用） | ❌ 不用 | 对未参数化组件无价值；v7 全量注册，池划分由 M2 registry 画像承担 |
| FakeDashboard / Card 骨架 / TitleBlock | ❌ 不用 | 演示占位，v7 不搬 |
| 参数化后的镜头（automotion/lenses 版） | ❌ 不用 | 参数化在 v7 重新设计，不继承 automotion 的批次 props 化与 013 默认值污染 |
| tplshots 模板场景（6 Scene + 8 wrapper） | ✅ 用 | 已入库 v7（含 three 依赖 + live 纹理素材）；Scene 保留 id 不参与匹配 |
| "Real" 族截图素材（projects-full.png 等） | ✅ 用 | 已入库 v7（public/textures）；Real 族镜头已更新为 video-shotcraft 重做版 |
| 旧 registry.json（28 条） | ❌ 不用 | 重写为 169 条全量画像 |
| 旧 client/App.tsx（编辑器） | ❌ 不用 | 重写为工作台 |
| 旧 server/ 渲染管线 | ✅ 参考用 | Express + Remotion renderer + 预览缓存思路保留，接口重写 |
| 旧 scripts/ 生成器与填参 | ⚠️ 参考 | 逻辑参考，按 v7 数据模型重写 |
| 013 storyboard.json（项目数据） | ✅ 用 | 作为 v7 首个项目的输入，参数只存在项目侧 |
| 013 旧副本 storyboard.json | ❌ 不用 | 过期副本，勿引用 |
| 纸墨项目（独立工程） | ❌ 不用 | v7 只有一套工程，纸墨退役（预览入口并入 v7） |

---

## 3. 镜头库（v7/lenses）

### 3.0 来源与状态（用户定义）

- v7 镜头库 = **纸墨风转换完成、参数化之前**的 163 个镜头（纸墨/src/compositions.bak：纸墨版 140 + video-shotcraft 后加 23 个已纸墨化）
- 每个镜头：G 纸墨色板已统一、无 props 接口（未参数化）
- **参数化是 v7 镜头库建设的一部分**：在纸墨版基础上重新设计每个镜头的参数 schema（中性默认值），不继承 automotion 的旧 props 化
- 中文命名（lens-names.md）+ 统一单一命名（id=组件名）保留
- automotion 新增的 2 个镜头（BeatStepListThemeCycle / DocTypewriter）不在纸墨版内，v7 单独评估是否重建纳入

### 3.1 注册与池划分

- 镜头库 = **全量注册**（169 个，含底层/辅助组件），不做「可用/疑似/不可用」静态分级——组件全部未参数化，分级无依据
- 「进匹配池与否」由 M2 registry 画像（contentCapable / sceneScope）在参数化完成后判定
- **底层/辅助**：Scene 6 个（底层组件）、VerticalTickerWrapper（辅助）——保留 id 不参与匹配。

### 3.2 参数化与中性化（v7 镜头库的硬性要求）

- 组件默认参数 = 中性通用示例，**禁止**写死任何项目内容（政策、红头、专有品牌）
- 项目内容一律通过 storyboard 参数传入
- 纸墨版未参数化，天然无污染；参数化设计时保持中性（旧版污染仅存在于 automotion 参数化版，v7 不搬）

### 3.3 色板与资源

- `G` 色板拆为独立文件（如 `lenses/_system/colors.ts`），组件 import 它
- 演示占位（FakeDashboard/Card/TitleBlock）不搬
- 静态资源按镜头是否保留决定：Real 族不救则不搬 projects-full.png / live-layout.json 等

---

## 4. 命名与注册

- id = 组件导出名（PascalCase，无连字符），全系统唯一，与目录名无关
- 目录名仅作代码组织，不参与命名
- 全量注册：`lenses/Root.preview.tsx`（146 个，含底层 Scene）——预览陈列柜入口
- 运行注册：按 registry 动态生成（gen-root 思路），保证注册与 registry 一致
- 中文名：一个镜头一个，显示用，不参与 id

---

## 5. 数据模型

### 5.1 registry（146 条全量画像）

每条镜头记录：

- id / 中文名 / 组件路径 / 分组
- contentCapable：`content`（正文池）/ `transition`（转场池）/ `demo`（禁入池）
- sceneScope：内容 / 转场 / 开场 / 结尾（正交于 contentCapable）
- functions / energy / density / excludeKeywords
- 视觉语义（一句话）+ canCarry（可承载：标题/列表/引文/卡片组/长文本/无）
- textRestriction（字形限制：无/仅拉丁大写）
- time（刚性/弹性帧段，**灵活组合**：可全刚性 / 全弹性 / 混合）+ preview（fixed/derive 预览策略）
- params（参数 schema + 中性默认值）+ mapFromSegment

### 5.2 storyboard（项目数据，单一权威）

- 段：id / text / keywords / phraseRange / durationSec（第一步由转录确定）/ lensId / params
- 段画像（title / role / functions / energy / density）：**推迟**到「段 ↔ 画面对齐方式」确定后（M2/M3）标注，第一步不做
- `role`（后续补标）：`content`（承载正文）/ `bridge`（纯过渡）/ `open` / `close`——决定候选池
- 镜头库与项目分离：storyboard 只存项目侧参数，镜头默认值永远中性

### 5.3 转录与字幕（第一步产物，时间基准）

- 词级转录文件：词/语义单元 + 起止时间戳 + 段归属 + 段内序号（一切时间计算的源头）
- 字幕文件：字幕条（序号 + 文本 + 起止时间 + 段归属），013 的 283 条为现成样本，供合成叠加
- 段真实时长 = 段内字幕时间跨度（首条开始 → 末条结束）
- 段内动效时间点：词/短语出现时间序列，镜头参数可引用（逐词填充 / 词列表滚动 / 口播同步）
- 字幕样式参数为项目级（全片统一），存 storyboard meta

---

## 6. 匹配系统

> ⚠️ 本节为蓝图草案：具体约束与分值**尚未定稿**，M3 设计时确定。已拍板的方向：硬约束 + 规则打分 + 理由；自动采用最高分，其余候选置顶；不用 LLM 对话选镜头。
> M3 目标：匹配机制需设计得足够好，弥补「段画像在第一步不标注」带来的输入缺失。

### 6.1 候选池（硬约束）

1. 段 role × 镜头 contentCapable/sceneScope（正文段只能从 content 池；bridge 段从 transition 池）
2. textRestriction：字形受限镜头只匹配英文缩写词段，否则排除
3. excludeKeywords 命中 → 排除
4. 时长兼容：刚性核心帧数 ≤ 段时长（不足则降权/排除）

### 6.2 打分（候选池内排序）

- 功能命中（+10/个）、能量相同（+5）、密度匹配（+3）
- 关键词 ↔ 视觉语义/canCarry 正向匹配（+5/命中）
- 输出：排序列表 + 一句理由（可审查）

### 6.3 输出形态

- 自动采用最高分镜头
- 其余候选按分数置顶在工作台"推荐镜头"区，可人工更换

### 6.4 明确不做

- 不用 LLM 对话直接选镜头
- 不用旧 scoreLens 的简单打分

---

## 7. 工作台（v7/client）

工作台 = 定稿台，分两个环节，数据衔接（环节一产出 → 环节二消费）。

### 7.1 环节一：分镜定稿（三栏）

- 左栏：段列表（段多时用滚动条选择）；中栏：参数表单 + 镜头预览；右栏：推荐区 + 镜头库
- 右栏 hover 镜头 → 弹出窗口展示效果，移开消失；右栏可伸缩，缩回后中栏（尤其预览窗口）等比放大
- 预览只展示镜头效果 + 参数调整（非真实时长）
- 左栏最下方固定两行按钮（滚动时不跑）：
  - 「保存当前效果」：保存镜头替换 + 参数修改（首次 = 打开以来全部改动，之后 = 上次保存以来的增量）
  - 「进入字幕设定」：进入环节二

### 7.2 环节二：字幕样式（两栏）

- 左栏（占大部分）：预览区——第一个镜头稳定后的画面（有刚性 → 刚性结束后弹性时段；无刚性 → 弹性任意时间点；仅刚性 → 刚性最后一帧），字幕文字用「字幕调整工作区」模拟真实字幕效果
- 右栏：字幕参数调整区（参考剪映：字号/字体/颜色/描边/背景/位置/对齐/字间距等）
- 右栏最下方两按钮：
  - 左「返回上一步」：回环节一
  - 右「确定当前设置」：弹二次确认对话框（确认/返回）；「返回」= 未点击；「确认」后提示可退出，skill 进入下一步
- 字幕文本与时间码由第一步转录自动生成，本环节只定样式（不做时间轴拖拽编辑）

### 7.3 明确不做

- 不做字幕/BGM/整片合成（那是 Remotion 的事）
- 不做 demo 镜头可随意选（准入约束强制）
- 不做字幕时间轴拖拽编辑（文本/时间码自动生成）

---

## 8. 渲染服务（v7/server）

- 复用思路：Express + @remotion/bundler + @remotion/renderer + 常驻浏览器 + 预览缓存 + RENDER_VERSION
- API：lenses（全量 146）/ storyboard / preview（按段渲染）/ render（整片，后续阶段）
- 预览缓存键：RENDER_VERSION | lensId | params | frames
- 预览策略：每个镜头单独预览渲染（效果预览）+ 最后一次合成总渲染（整片预览）
- 增量原则：整片预览发现问题 → 回工作台改对应镜头 → 只重渲染该镜头预览，其余复用缓存

---

## 9. 最终合成（Remotion，独立阶段）

- 消费权威 storyboard.json + 字幕数据
- 整片组装：段 Sequence 串联 + 字幕层（283 SRT）
- 与工作台解耦：工作台定稿后交给它
- 流程角色：第三步 = 整片预览（验收节点）；第四步 = 导出最终视频
- v7 当前阶段不引入 BGM

---

## 10. v7 目录结构（目标）

```
automotion-v7/
├── lenses/              # 唯一镜头库（组件 + Root.preview.tsx + _system/colors）
├── engine/              # 时间轴引擎
├── shared/              # registry.json（146 全量）+ types.ts
├── match/               # 匹配系统（约束 + 打分 + 理由）
├── client/              # 工作台
├── server/              # 渲染服务
├── storyboards/         # 项目 storyboard.json（每项目一份）
└── docs/                # 文档（含本组建文档、命名表）
```

---

## 11. 执行里程碑

- **M0 骨架**：建 v7 工程（目录 / git / package.json / workspace），旧 automotion 打 tag 冻结
- **M1 镜头库**：140 纸墨版镜头入库 + 命名统一（id=组件名 + 中文名）+ G 色板拆出 + 参数化设计（中性默认）+ 全量预览跑通（陈列柜干净）
- **M2 数据模型**：registry 146 条画像 + storyboard role 标注（32 段）
- **M3 匹配系统**：候选池约束 + 打分 + 理由
- **M4 工作台**：UI + 参数联动 + 预览策略
- **M5 最终合成**：整片组装（字幕）接入（BGM 按当前决策暂不引入）

每个里程碑有明确验证点（M1 = 3000 端口预览全量中性、M3 = 匹配结果可审查、M4 = 换镜头自动填参）。

---

## 12. 明确不做的（v6 病根清单）

1. 双项目拷贝（纸墨 + automotion）——v7 单一代码源
2. 目录名/组件名/注册表三套命名——v7 id 唯一
3. 组件默认值写项目内容——v7 中性化
4. 28 镜头注册池——v7 全量 146
5. 换镜头不联动参数——v7 自动重填
6. LLM 对话选镜头——v7 规则匹配 + 理由
7. 演示占位（FakeDashboard/Card/TitleBlock）——v7 剔除
8. docs 提交混乱——v7 文档即代码，纳入 git
