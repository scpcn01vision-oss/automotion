# automotion-v7 skill 构建蓝图

> 生成日期：2026-08-04（2026-08-06 阶段 1 重写）
> 定位：automotion-v7 skill 构建蓝图。本文档判定旧 automotion（v6）及周边资产**哪些用、哪些不用、哪些新写**，覆盖全流程（镜头库 → 数据模型 → 匹配 → 工作台 → 渲染 → 最终合成）。
> 最终目标：构建 automotion-v7 —— **通用的、自动化的口播视频成片 skill**。用户提供「文案 + 录音」，skill 走完「转录/分段 → 工作台定稿 → 整片预览 → 导出」，产出**任意主题口播视频**成片（1920×1080、30fps、mp4、自然时长、带字幕、无 BGM）。
> 原则：单一代码源、中性镜头库、项目数据分离、可审查流程、文档即代码。

### 已拍板决策（执行时遵守，不推翻）

- 输入 = 文案 + 录音；范围 = 任意主题口播视频
- 风格 = 纸墨风固定；`lenses/_system/` 为唯一风格权威（色板 colors.ts / 字体 typography.ts）
- 输出 = 1920×1080 横屏、30fps、mp4、自然时长、无 BGM
- 自动化边界：转录/分段/匹配/导出自动；工作台定稿 + 整片预览验收为人工
- 匹配 = 无准入/无排除/无禁入，纯匹配度；**M3 专题讨论，不预设机制**
- 「可用/疑似/不可用」分级彻底抛弃
- 项目数据（013 等）不进 v7 仓库；测试用隔离环境真实文案（项目侧 `E:\桌面\打破信息差\视频文件\013B`），不做中性假样本
- 路线：修不重做；工作台交互 = 保存式（非向导式）

---

## 1. 全流程总览（v7 数据流）

```
第一步：文案处理 + 录音处理（并行）
  ├ 文案：文案 → 分段（段含 phraseRange）→ 段文本列表
  └ 录音：口播录音 → 词级转录（字词语义 + 时间戳）→ 字幕分割 → 转录文件 + 字幕文件
  汇合：段文本 ↔ 词级转录对齐 → 每段真实时长（第一步定死）
第二步：工作台匹配定稿
  匹配（机制 M3 定稿）→ 段×镜头×参数 + 字幕样式定稿 → storyboard.json（权威）
第三步：Remotion 整片预览（画面 + 字幕，无 BGM）——验收节点，不通过回第二步只重渲对应镜头
第四步：导出最终视频
```

- 工作台 = 定稿工具（浏览器网页），分「分镜定稿」「字幕样式」两环节：只负责段→镜头→参数 + 字幕样式
- Remotion = 预览与最终执行（整片预览 / 导出），不参与选镜头
- 镜头库 = 中性资产库，不含任何项目内容
- 时长基础：每段真实时长由第一步的转录确定；镜头时长 = 刚性（不可压缩）+ 弹性（可伸缩）**灵活组合**，总时长对齐段时长
- v7 当前阶段不引入 BGM

---

## 2. 资产判定总表

| 资产 | 判定 | 说明 |
|---|---|---|
| 120 个镜头组件源码（纸墨版 140 + video-shotcraft 后加 23 个已纸墨化，历经阶段删减后现存 120 个 tsx；均已参数化） | ✅ 用 | 来源：纸墨/src/compositions.bak；搬入 v7/lenses，全量注册 124 个 Composition |
| G 纸墨色板（颜色常量） | ✅ 用 | `lenses/_system/colors.ts` 独立，唯一风格权威 |
| 字体栈 | ✅ 用 | `lenses/_system/typography.ts` 的 FONT_STACK（阶段 1 收敛 98 文件）；镜头局部变体保留为镜头表现 |
| engine/time.ts + useShotFrame.ts | ✅ 用 | 时间轴引擎（刚性/弹性），位于根 `engine/`（阶段 1 迁移） |
| 命名表（docs/lens-names.md） | ✅ 用 | id=组件名 + 中文名 + 组（140 条） |
| 旧静态分级（可用 11 / 疑似 17 / 不可用） | ❌ 不用 | 彻底抛弃，无「可用/疑似/不可用」概念 |
| FakeDashboard / Card 骨架 / TitleBlock | ❌ 不用 | 演示占位，v7 不搬；阶段 1 已从 `_fixtures` 剥离，仅保留 G 中转 |
| 参数化后的镜头（automotion/lenses 版） | ❌ 不用 | 参数化在 v7 重新设计，不继承旧 props 化与项目默认值污染 |
| tplshots 模板场景（6 Scene + 8 wrapper） | ✅ 用 | 已入库 v7（含 three 依赖 + live 纹理素材）；Scene 保留 id 不参与匹配 |
| "Real" 族截图素材（projects-full.png 等） | ✅ 用 | 已入库 v7（public/textures）；Real 族镜头为 video-shotcraft 重做版 |
| 旧 registry.json（28 条） | ❌ 不用 | 重写为 124 条全量注册（M2） |
| 旧 client/App.tsx（编辑器） | ❌ 不用 | 重写为工作台 |
| 旧 server/ 渲染管线 | ✅ 参考用 | Express + Remotion renderer + 预览缓存思路保留，接口重写 |
| 旧 scripts/ 生成器与填参 | ⚠️ 参考 | 逻辑参考，按 v7 数据模型重写 |
| 013 storyboard.json（项目数据） | ❌ 不进仓库 | 项目数据只存在项目侧，作为端到端验收输入 |
| 纸墨项目（独立工程） | ❌ 不用 | v7 只有一套工程，纸墨退役（预览入口并入 v7） |

---

## 3. 镜头库（v7/lenses）

### 3.0 来源与状态（用户定义）

- v7 镜头库 = 纸墨风转换完成并参数化的 **120 个镜头**（纸墨/src/compositions.bak：纸墨版 140 + video-shotcraft 后加 23 个已纸墨化，历经阶段删减后现存 120 个）
- 每个镜头：G 纸墨色板统一、`XxxProps` 接口（117/120）、中性默认值
- 中文命名（docs/lens-names.md，140 条）+ 统一单一命名（id=组件名）保留
- automotion 新增的 2 个镜头（BeatStepListThemeCycle / DocTypewriter）不在纸墨版内，v7 单独评估是否重建纳入（M2）

### 3.1 注册

- 镜头库 = **全量注册**（`lenses/Root.preview.tsx` 124 个 Composition，含底层 Scene 6 + 辅助 VerticalTickerWrapper）
- 无「进匹配池与否」概念——匹配不做准入/排除/禁入，机制在 M3 专题讨论
- **底层/辅助**：Scene 6 个（底层组件）、VerticalTickerWrapper（辅助）——保留 id 不参与匹配

### 3.2 参数化与中性化（v7 镜头库的硬性要求）

- 组件默认参数 = 中性通用示例，**禁止**写死任何项目内容（政策、红头、专有品牌、销售数据形态文案）
- 项目内容一律通过 storyboard 参数传入
- 阶段 1 已完成：8 个真实使用演示组件的镜头换中性渲染；品牌默认值中性化；色板值字面复制清理；污染扫描 0 命中

### 3.3 色板、字体与资源

- `G` 色板独立于 `lenses/_system/colors.ts`（唯一风格权威），组件统一引用
- 字体收敛于 `lenses/_system/typography.ts`（FONT_STACK）；镜头局部变体保留并记录
- 演示占位（FakeDashboard/Card/TitleBlock）已剥离，`lenses/_fixtures/Fixtures.tsx` 仅作 G 中转（152 个文件 import 不动）
- 静态资源按镜头是否保留决定（Real 族保留，public/textures）

---

## 4. 命名与注册

- id = 组件导出名（PascalCase，无连字符），全系统唯一，与目录名无关
- 目录名仅作代码组织，不参与命名
- 全量注册：`lenses/Root.preview.tsx` **124** 个 Composition（含底层 Scene 6 + 辅助 VerticalTickerWrapper）
- 运行注册：按 registry 动态生成（gen-root 思路），保证注册与 registry 一致
- 中文名：一个镜头一个，显示用，不参与 id
- 数字口径：**注册 124 / 命名表 140 / 组件 120**（2026-08-07 参数化删减后复核；旧「165 / 167 / 162」及更早「146 / 169」口径作废）

---

## 5. 数据模型（M2 重定义）

> M2 从零设计，**不预设字段、不引 013**。以下为本蓝图方向，具体 schema 在 M2 开工时与用户对齐。

### 5.1 registry（shared/registry.json，124 全量）

- id / 中文名 / 组件路径 / 分组（注册元数据，M2 必含）
- 参数 schema（从组件 `XxxProps` 自动提取）
- **语义画像字段（匹配相关）留给 M3**——不预设 contentCapable / sceneScope / excludeKeywords 等带「池/禁入/排除」语义的字段

### 5.2 storyboard（项目数据，单一权威，项目侧）

- 段：id / text / keywords / phraseRange / durationSec（第一步由转录确定）/ lensId / params
- 镜头库与项目分离：storyboard 只存项目侧参数，镜头默认值永远中性

### 5.3 转录与字幕（第一步产物，时间基准）

- 词级转录文件：词/语义单元 + 起止时间戳 + 段归属 + 段内序号（一切时间计算的源头）
- 字幕文件：字幕条（序号 + 文本 + 起止时间 + 段归属）
- 段真实时长 = 段内字幕时间跨度（首条开始 → 末条结束）
- 段内动效时间点：词/短语出现时间序列，镜头参数可引用（逐词填充 / 词列表滚动 / 口播同步）
- 字幕样式参数为项目级（全片统一），存 storyboard meta
- 测试数据用隔离环境真实文案（项目侧 013B），不进仓库

---

## 6. 匹配系统（方向声明；机制 M3 专题讨论）

> ⚠️ 本节刻意留空机制：M3 是最高优先级专题，开工时从零讨论，**不预设任何机制**。已对齐的方向约束：

- **无准入 / 无排除 / 无禁入**：不存在「能不能用」的概念，只有匹配度
- 匹配结果可审查（带理由）
- 不用 LLM 对话直接选镜头
- 不用旧 scoreLens 的简单打分

具体机制（输入特征、打分模型、输出形态）在 M3 开工时专门讨论后写入本节。

---

## 7. 工作台（v7/client）

工作台 = 定稿台，分两个环节，数据衔接（环节一产出 → 环节二消费）。**保存式交互**（非向导式）：镜头调整与字幕调整均有「保存」按钮，保存一次更新一次文件；「进入下一步」由用户与 skill 的对话交互决定。

### 7.1 环节一：分镜定稿（三栏）

- 左栏：段列表（段多时用滚动条选择）；中栏：参数表单 + 镜头预览；右栏：推荐区 + 镜头库
- 右栏 hover 镜头 → 弹出窗口展示效果，移开消失；右栏可伸缩，缩回后中栏（尤其预览窗口）等比放大
- 预览只展示镜头效果 + 参数调整（非真实时长）
- 左栏最下方固定两行按钮（滚动时不跑）：「保存当前效果」「进入字幕设定」

### 7.2 环节二：字幕样式（两栏）

- 左栏（占大部分）：预览区——第一个镜头稳定后的画面，字幕文字用「字幕调整工作区」模拟真实字幕效果
- 右栏：字幕参数调整区（参考剪映：字号/字体/颜色/描边/背景/位置/对齐/字间距等）
- 右栏最下方两按钮：「返回上一步」「确定当前设置」（弹二次确认对话框；「返回」= 未点击；「确认」后提示可退出，skill 进入下一步）
- 字幕文本与时间码由第一步转录自动生成，本环节只定样式（不做时间轴拖拽编辑）

### 7.3 明确不做

- 不做字幕/BGM/整片合成（那是 Remotion 的事）
- 不做字幕时间轴拖拽编辑（文本/时间码自动生成）

---

## 8. 渲染服务（v7/server）

- 复用思路：Express + @remotion/bundler + @remotion/renderer + 常驻浏览器 + 预览缓存 + RENDER_VERSION
- API：lenses（全量 124）/ storyboard / preview（按段渲染）/ render（整片，后续阶段）
- 预览缓存键：RENDER_VERSION | lensId | params | frames
- 预览策略：每个镜头单独预览渲染（效果预览）+ 最后一次合成总渲染（整片预览）
- 增量原则：整片预览发现问题 → 回工作台改对应镜头 → 只重渲染该镜头预览，其余复用缓存

---

## 9. 最终合成（Remotion，独立阶段）

- 消费权威 storyboard.json + 字幕数据（转录产物）
- 整片组装：段 Sequence 串联 + 字幕层
- 与工作台解耦：工作台定稿后交给它
- 流程角色：第三步 = 整片预览（验收节点）；第四步 = 导出最终视频
- v7 当前阶段不引入 BGM

---

## 10. v7 目录结构（目标）

```
automotion-v7/
├── lenses/              # 唯一镜头库（组件 + Root.preview.tsx + _system + _fixtures(G 中转)）
│   └── _system/         # 唯一风格权威：colors.ts（色板）/ typography.ts（字体）/ 中性渲染器
├── engine/              # 时间轴引擎（time.ts / useShotFrame.ts，根级共享）
├── shared/              # registry.json（124 全量，M2）+ types.ts
├── match/               # 匹配系统（机制 M3 定稿）
├── client/              # 工作台
├── server/              # 渲染服务
├── scripts/             # 验证与扫描（verify-lenses.mjs / scan-pollution.mjs）
└── docs/                # 文档（本组建文档、命名表、参数化规范、方案、交接说明）
```

---

## 11. 执行里程碑

- **M0 骨架** ✅：建 v7 工程（目录 / git / package.json / workspace），旧 automotion 打 tag 冻结
- **M1 镜头库** ✅：120 组件入库（历经删减）+ 命名统一（140 条）+ G 色板拆出 + 参数化（117/120 Props）+ 全量预览跑通（124/124，2026-08-07 复核）
- **阶段 1（M1 修复）** ✅：污染清理（8 镜头中性渲染 / 12 import / 品牌中性化 / Fixtures 收尾 / 23 处乱码注释）+ 风格收敛（字体 FONT_STACK / 色板值复制清理）+ engine 迁移到根 + scan-pollution.mjs（0 命中）+ 组建文档重写
- **M2 数据模型**：shared/types.ts（镜头注册 / storyboard / 字幕 / 转录 schema）+ registry.json（124 全量，参数 schema 自动提取）+ 隔离环境真实文案测试（项目侧 013B）；语义画像字段留给 M3
- **M3 匹配系统** ⚠️ 专题讨论：无准入/无排除/无禁入，纯匹配度，可审查理由；机制从零讨论后写入 §6
- **M4 工作台**：三栏布局 + 两环节 + 保存式交互 + 预览（@remotion/player + 单镜头渲染 + 增量缓存）；验证点：换镜头自动填参
- **M5 最终合成**：storyboard + 字幕 → 整片 Sequence + 字幕层 → 整片预览（验收）→ 导出 mp4（1920×1080@30，无 BGM）；验证点：端到端跑通（用户真实录音，项目侧）
- **M6 skill 封装**：skill-creator 封装 SKILL.md + scripts/references/assets，放 `~/.codex/skills/`；验证点：quick_validate 通过 + 一次真实任务走通

---

## 12. 明确不做的（v6 病根清单）

1. 双项目拷贝（纸墨 + automotion）——v7 单一代码源
2. 目录名/组件名/注册表三套命名——v7 id 唯一
3. 组件默认值写项目内容——v7 中性化
4. 28 镜头注册池——v7 全量 124
5. 换镜头不联动参数——v7 自动重填
6. LLM 对话选镜头——v7 规则匹配 + 理由（机制 M3 定稿）
7. 演示占位（FakeDashboard/Card/TitleBlock）——v7 剔除
8. 「可用/疑似/不可用」静态分级——v7 彻底抛弃
9. docs 提交混乱——v7 文档即代码，纳入 git

---

## 13. 执行规范（防复发）

阶段 1 执行中踩过的坑与固化的机制，后续所有批量操作必须遵守：

1. **批量文件修改只用 Node 脚本 + 相对路径**（绝对路径含中文时 PowerShell→Node 管道会编码损坏；相对路径绕开）
2. **每次替换打印「替换对 + 命中数」且替换前做命中数断言**（替换对以 JSON 配置声明；命中数与预期不符立即中止，不继续）
3. **任何批量改动后必跑全量 bundle（esbuild Root.preview）+ diff 抽查**，不能只看日志
4. **禁止 PowerShell 数组嵌套传参做批量替换**（内层数组会被 flatten，`$p[0]` 退化为字符索引——阶段 1 曾因此损坏 12 个文件，已从 HEAD 恢复）
5. 扫描器（scan-pollution.mjs）任何词表调整后，先造阳性样本验证能命中，再跑全库确认 0 命中
