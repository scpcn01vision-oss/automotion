# 新对话提示词：M4 工作台 debug 与优化

> 用法：把下方整段复制给新对话作为第一条消息。

---

你是 automotion-v7 项目（`E:\桌面\打破信息差\automotion-v7`）的执行者。请先**完整阅读**以下文件，再开始工作：

1. `docs/交接说明-M4工作台-2026-08-10.md` —— 工作台当前状态、启动方式、已实现功能、**已知问题清单（debug 方向）**、已拍板决策、验证基线
2. `docs/工作台需求-M4.md` —— 工作台功能需求（三栏/两环节/保存式/hover 无延迟动效预览等）
3. `docs/匹配机制-M3.md` —— 匹配机制（Top5 + reason；自动填参的参考依据）
4. `docs/交接说明-2026-08-06.md` §3 —— 项目基础已拍板决策

**当前基线**：main = `dbb2220`（M4 工作台完成，工作区干净）。请先建分支 `codex/m4-workbench-debug` 再开工。

**启动方式**：
```powershell
cd E:\桌面\打破信息差\automotion-v7
$env:V7_PROJECT_DIR = "E:\桌面\打破信息差\视频文件\013B"
npm run dev
```
浏览器打开 http://localhost:5173（client）+ 3004（server）。013B 项目侧 `storyboard.json`（32 段，参考答案镜头 + 对应文案参数）已生成。

**本次目标：工作台 debug 与优化**，按优先级：

1. **修图片解码失败**：PageWaterfallWall 等使用 `public/textures/` 静态图的镜头，在 Player 环境报 `EncodingError: The source image cannot be decoded`——定位 staticFile 资源在 vite dev / @remotion/player 下的路径问题并修复
2. **一般化自动填参**：当前 013B 用「理想镜头参考答案-013B.md」预生成 storyboard（文案 → 镜头 → 参数）；沉淀「文案 → 参数」的自动映射规律，让其他项目也能自动填参（工作台不做 AI；生成动作由 skill/流程完成，工作台只展示与预览）
3. **逐项体验验证**：hover 悬浮窗动效（无延迟）、数组/嵌套参数列表行编辑、深层 JSON 兜底、字幕环节、保存/手动刷新、未定稿段自动预选 Top1

**必须遵守的规则**：
- 所有文件写入/删除必须先征得我同意再执行；不要在我问「这是什么/为什么」之后顺手执行
- 回答用中文
- 我的指令有模糊部分先追问，不猜测执行
- 诊断性问题（为什么/怎么会/是什么）只做解释，结尾明确询问是否要修；只有我回复「修/改/执行」后才动手
- 合并、提交、分支、工作树操作必须得到我许可
- 验证基线：每步改动后 `npx tsc --noEmit` 0 错误、client `vite build` 通过；不破坏 `validate-m2` / `scan-pollution`（0 命中）/ 全量渲染（124/124）
- 013B 数据在项目侧隔离环境，不进仓库

请先读文件，然后给出你的 debug/优化执行计划，等我确认后开始。
