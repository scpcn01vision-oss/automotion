# automotion-v7 新对话提示词

> 用法：把下面「提示词」分隔线内的内容完整复制，作为新对话的第一条消息发出。

---

你是 automotion-v7 项目的执行者。请先**完整阅读**以下三份文件：

1. `docs/交接说明-2026-08-06.md` —— 当前状态、已拍板决策、已知问题、规则
2. `docs/skill-构建方案-2026-08-06.md` —— 6 阶段执行路线（含验证点）
3. `v7-组建文档.md` —— 领域蓝图（阶段 1.4 会重写它）

目标：构建 automotion-v7 这个「通用的、自动化的口播视频成片 skill」。本次从**阶段 1（M1 修复）**开始执行，之后按方案推进 M2 → M3 → M4 → M5 → 最终封装 skill。

## 必须遵守的规则

- 所有文件写入/删除，必须先征得我同意再执行；不要在我问「这是什么/为什么」之后顺手就做了
- 回答用中文
- 我的指令有模糊部分，先追问搞清楚，不要猜测执行
- 对于绝对明确的指令，可以使用子代理；指令模糊时必须先追问，不得用子代理绕过确认
- 诊断性问题（为什么/怎么会/是什么）只做解释，结尾明确询问是否要修；只有我回复「修/改/执行」这类明确指令后才动手

## 已拍板决策（执行时遵守，不要推翻）

- 输入 = 文案 + 录音；范围 = 任意主题口播视频
- 风格 = 纸墨风固定；`_system/colors.ts` 是唯一风格权威；`_fixtures` 保留 G 色板中转；来源注释保留
- 输出 = 1920×1080 横屏、30fps、mp4、自然时长、无 BGM
- 自动化边界：转录/分段/匹配/导出自动；工作台定稿 + 整片预览验收为人工
- 匹配：**无准入/无排除/无禁入**；M3 是刻意留空的专题——执行到阶段 3 时必须停下，回来和我专门讨论，不预设任何机制
- 「可用/疑似/不可用」分级彻底抛弃
- 013 等具体项目数据不进 v7 仓库；测试用隔离环境真实文案（项目侧 013B）；端到端验收用我提供的真实录音（项目侧）
- 修不重做
- 工作台交互：保存式（非向导式），镜头/字幕各有保存按钮，进入下一步由我与 skill 的对话决定

## 阶段 1 任务

按 `docs/交接说明-2026-08-06.md` §4 的问题清单处理：

1. 8 个镜头真实使用演示组件 → 换中性渲染
2. 11 个残留 import → 删
3. 4-5 处品牌默认值 → 换中性
4. `remotion.config.ts` 中文注释乱码 → 修复
5. engine 从 `lenses/engine/` 迁到根 `engine/`（改 CardFlipReveal import）
6. 新增 `scripts/scan-pollution.mjs` 污染扫描
7. 按 spec §1.4 重写 `v7-组建文档.md`

每完成一小步，先给我看改动再执行下一步。第一步：读完文件后，给出阶段 1 的详细执行计划（第一小步：污染清理清单），等我的确认再动手。

开始吧。

---
