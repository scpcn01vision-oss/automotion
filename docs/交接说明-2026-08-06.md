# automotion-v7 交接说明

> 日期：2026-08-06
> 用途：给接手新对话的完整交接。新对话从本文件的「起点」读起，按 spec 执行。

## 1. 新对话起点

开新对话后，先读这两份文档，然后按 spec 从**阶段 1（M1 修复）**开始执行：

1. `docs/skill-构建方案-2026-08-06.md` —— 执行路线（6 阶段 + 验证点）
2. `v7-组建文档.md` —— 领域蓝图（阶段 1.4 将按 spec 重写它）

建议的第一条消息：「读 docs/skill-构建方案-2026-08-06.md 和 v7-组建文档.md，按方案从阶段 1 开始执行，所有文件写入/删除前先征得我同意。」

## 2. 当前状态

- git：`main` HEAD = `af3dee8`（工作区干净；spec 与交接说明写入后未提交，待用户确认）
- 旧 automotion：已打 tag `legacy-v6-20260804` 冻结（分支/工作区未动）
- M0 ✅：骨架（npm workspace：client/server/lenses/match）+ 组建文档初版
- M1 ⚠️ 完成但带残留：165 注册 / 167 命名 / 162 组件文件；151/162 带 XxxProps；全量渲染 165/165 通过（`out/verify-report.json`，2026-08-06 复核）；**但「演示痕迹清零」声明不实**——残留清单见 §4
- M2-M5 未开始；`shared/`、`match/`、`samples/` 均空

## 3. 已拍板决策（不要推翻，执行时遵守）

| 项 | 决策 |
|---|---|
| 输入 | 文案 + 录音 |
| 范围 | 任意主题口播视频 |
| 风格 | 纸墨风固定；`_system/colors.ts` 唯一风格权威；`_fixtures` 保留 G 中转；来源注释保留 |
| 输出 | 1920×1080 横屏、30fps、mp4、自然时长、无 BGM |
| 自动化 | 转录/分段/匹配/导出自动；分段可在工作台人工调整 |
| 人工 | 工作台定稿（分镜 + 字幕样式）+ 整片预览验收（发现问题回工作台改） |
| 匹配 | 无准入/无排除/无禁入，纯匹配度；**M3 专题讨论，spec 留空不预设** |
| 可用性分级 | 彻底抛弃 |
| 项目数据 | 013 等具体项目数据不进仓库；试验用隔离中性样本；端到端验收用用户真实录音（项目侧） |
| 路线 | 修不重做 |
| 工作台交互 | 保存式（非向导式）：镜头/字幕各有保存按钮，保存一次更新一次文件；进入下一步由用户与 skill 对话决定 |

## 4. 已知问题（阶段 1 处理清单）

1. **8 个镜头真实使用演示组件**：TitleBlock ×3（CelFlashStomp / MaskingTapeSlap / RisoMisregistrationHit）；Card/FakeDashboard 相关 5 个（LineUnfoldPanel / KeycapSmashCut / AttentionBounce / GridWaveFlip / UiStripAwayOutro）→ 换中性渲染
2. **11 个残留 import**（`import { G, TitleBlock }` 未使用）→ 删
3. **4-5 处品牌默认值**：`Notion AI`（MarkerUnderlineTitle）、`perplexity`（IconFlipBloomLogo）、`ClickUp Space`（IntegrationHubMap）、`Find in Slack`（PillSlotCycle）等 → 换中性
4. **`remotion.config.ts` 中文注释乱码**（GBK）→ 修复
5. **engine 位置**：现于 `lenses/engine/`，需迁到根 `engine/`（共享资产；改 CardFlipReveal 的 import）
6. **验证盲区**：`verify-lenses.mjs` 只验渲染不验污染 → 新增 `scripts/scan-pollution.mjs`，M1 验收 = 扫描 0 命中 + 渲染 165/165 + 用户抽查 8-10 个镜头

## 5. 规则（用户明确要求，必须遵守）

- 所有文件写入/删除必须先征得用户同意，不得顺手执行
- 回答用中文
- 指令模糊先追问，不猜测执行
- 对于绝对明确的指令，可以使用子代理；指令模糊时先追问，不得用子代理绕过确认
- 诊断性问题（为什么/怎么会/是什么）只解释，结尾明确问是否要修；用户说「修/改/执行」后才动手

## 6. 开放议题

- **M3 匹配机制**（最高优先级）：执行到阶段 3 时停下，回来与用户专门讨论，**不预设任何机制**
- skill 触发词与用户交互细节：阶段 6 定
- 端到端验收素材：用户提供真实录音（项目侧，不进仓库）

## 7. 其他

- `out/` 为验证临时产物（.gitignore 忽略），`out/verify-report.json` 是最近一次全量渲染报告
- 预览入口：`npm run preview` → 3003 端口（Mineradio 占用 3000，勿改）
- 相关旧资产位置（只参考，不引入 v7 仓库）：013 storyboard = `E:\桌面\打破信息差\automotion\storyboards\storyboard.json`；013 字幕 = `E:\桌面\打破信息差\视频文件\013\字幕.srt`
