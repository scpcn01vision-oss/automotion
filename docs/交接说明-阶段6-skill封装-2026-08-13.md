# automotion-v7 交接说明（阶段 6 skill 封装完成）

> 生成：2026-08-13
> 用途：新对话接手「013B forward-testing 迭代」与「GitHub 发布」两项收尾任务。
> 仓库：`E:\桌面\打破信息差\automotion-v7`（main 分支，工作区干净）

## 当前状态

- **M1-M5 全部完成**：污染清理 → 数据模型 → 匹配系统 → 工作台 → 整片合成，均已合并 main（HEAD = `b9fea63`）
- **阶段 6 skill 封装完成**：`C:\Users\AbstinEnce\.codex\skills\automotion-v7\`（已被 Codex 自动发现）
  - `SKILL.md`：四步流程（转录 → 匹配 Top5 → 工作台定稿 → 整片合成导出）+ 交互约定
  - `references/` 六份：匹配机制-M3 / 字幕切分规范-M5 / 工作台需求-M4 / 整片合成方案-M5 / 自动填参-映射规律-M4 / 参数化设计规范
  - `agents/openai.yaml`：UTF-8 元数据
  - `quick_validate.py` 已修编码 bug（read_text 显式 utf-8），官方验证通过
- 工具仓库：122 镜头、工作台（client/server）、整片合成（WholeVideo + 音频路由）、转录脚本（scripts/transcribe.py）

## 已拍板决策（勿推翻）

- 输入 = 文案 + 录音；输出 = 1920×1080@30 mp4、带字幕与口播、无 BGM
- 匹配 = 无准入/无排除/无禁入，Top5 + reason，人工定稿；驳回修定位不建黑名单
- 工作台 = 保存式交互；进入下一步由用户与 skill 对话决定
- 项目数据（013B 等）只在项目侧隔离环境，不进仓库；测试用 013B 真实文案
- **发布形态 A**：工具仓库 automotion-v7 发 GitHub + skill 独立发布；skill 自包含（references 全收）
- 所有文件写入/删除、分支/提交/合并前先征得用户同意；诊断性问题只解释、结尾问是否要修

## 待办 A：013B forward-testing（skill 迭代）

用 skill 实际跑一遍 013B 流程，验证 SKILL.md/references 是否覆盖真实操作，发现卡点反馈迭代：

- 新对话用 `$automotion-v7` 触发，走完整流程（转录 → 匹配 → 工作台定稿 → 整片合成）
- 013B 项目侧数据：`E:\桌面\打破信息差\视频文件\013B\`（storyboard.json / full.wav / transcript.json / subtitles.json / 参考答案）
- 对照检查：SKILL.md 每一步指令是否可执行、references 是否在正确时机被读、缺什么说明
- 反馈卡点 → 主对话/新对话更新 SKILL.md 或 references → 再测

## 待办 B：GitHub 发布

**工具仓库发布前检查**（已发现缺口）：

- ❌ 无 `README.md`（需补：项目简介、安装、启动方式、架构说明）
- ❌ 无 git remote（需用户提供 GitHub 仓库地址或新建）
- ✅ 无项目数据残留（013B 在项目侧）；.gitignore 已含 node_modules/out/dist/__pycache__
- 建议确认 license（工具仓库发布用）

**skill 发布**：`C:\Users\AbstinEnce\.codex\skills\automotion-v7\` 目录内容（SKILL.md + references + agents）即 skill 仓库内容；发布时可在 GitHub 仓库加 README（注意 skill 本身不含多余文档——README 只在 GitHub 仓库层，不放 skill 目录内）

## 规则与教训

- 回答用中文；指令模糊先追问；写入/删除/分支/提交/合并先征得用户同意
- 013B 数据不进仓库；`out/` 为验证临时产物
- 教训：重建镜头先读原版源码 + 配方卡；音频真实时长是合成总时长唯一基准；验证用响度检测不能只看有音轨
- 操作纪律（2026-08-13 forward-testing 教训）：写回验证前必须先备份目标文件；含中文 JSON 的 API 写操作禁用 PowerShell（编码会损坏中文，改用浏览器 / node fetch / curl + UTF-8 文件）；013B storyboard.json 无备份源，改动前先复制一份

---

## 建议的新对话提示词

见 `docs/新对话提示词-阶段6-2026-08-13.md`
